# Data

Raw data belongs in `data/raw/` and processed data belongs in `data/processed/`.

The training script downloads the Telco Customer Churn CSV automatically when `data/raw/WA_Fn-UseC_-Telco-Customer-Churn.csv` is missing. Raw data should remain unchanged so the pipeline can be reproduced from the original source.
